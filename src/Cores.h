#ifndef CORES_H       
#define CORES_H       

/*
 * Matriz de cores global
 * 
 * Declaração da matriz que armazena a paleta de cores do jogo no formato RGB.
 * - Primeira dimensão: 9 cores disponíveis (índices 0-8)
 * - Segunda dimensão: 3 componentes (R, G, B) em formato float (0.0-1.0)
 * 
 * A implementação/definição real está no arquivo Cores.cpp
 */
extern float cores[9][3];

#endif               // Fim do bloco condicional